{
  "name": "vibegrid",
  "version": "1.0.0",
  "description": "Mobile-first AI JRPG Engine",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "build": "esbuild main.js --bundle --outfile=dist/bundle.js --format=esm",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
